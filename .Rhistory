getwd()
dir()
data <- read.csv("activity.csv")
names(data)
head(data)
?summarize
ddply(dt,~group,summarise,mean=mean(age),sd=sd(age))
ddply(dt,~group,summarise,mean=mean(age),sd=sd(age))
library(dplyr)
group <- group_by(data, group)
names(ddata)
names(data)
group <- group_by(data, date)
group
group <- group_by(data, date)
names(data)
group <- group_by(data, data$date)
group <- group_by(data, date)
group
summarise(group, mean=mean(steps), sd=sd(steps))
summarise(group, mean=mean(steps), median=median(steps))
?summarise
data <- read.csv("activity.csv")
## Mean total number of steps taken per day?
library(dplyr)
summarise(group_by(data, date), mean=mean(steps), median=median(steps))
View(data)
summarie <-summarise(group_by(data, date), mean=mean(steps), median=median(steps))
View(summarie)
View(summarie)
summarie <-summarise(group_by(data, date), sum=sum(steps), mean=mean(steps), median=median(steps))
?plot
names(summarie)
plot(summarie$date, summarie$sum)
?plot
plot(summarie$date, summarie$sum, type="h")
plot(summarie$date, summarie$sum, type="h")
plot(summarie$date, summarie$sum, type="l")
plot(summarie$date, summarie$sum, type="l")
?plot
plot(summarie$date, as.numeric(summarie$sum), type="l")
plot(summarie$sum, type="l")
plot(summarie$sum, type="l",xlab="WeekDay")
plot(summarie$sum, type="l",xlab="WeekDay", ylab="Number of steps")
summarie <-summarise(group_by(data, date), sum=sum(steps))
plot(summarie$sum, type="l",xlab="WeekDay", ylab="Number of steps")
nas <- is.na(summarie$sum)
nas
mean_steps <- mean(summarie$sum[!nas])
median_steps <- median(summarie$sum[!nas])
median_steps <- median(summarie$sum)
median_steps <- median(summarie$sum[!nas])
?mean
?median
median_steps
mean_steps
